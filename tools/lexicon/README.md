# Lexicon ledger

`source-ledger.json` is the machine-readable record for lexicon inputs. The
current local crossword records the preferred external word list, its retrieval
date, the source's stated license status, the normalization transformation, and
the digest of the 78 emitted answer records. The source repository states no
license, so the artifact remains owner-review-required for public distribution.
